# Art Gallery Database Management System 🎨

A command-line database application built with **Python and MySQL** to manage artworks, artists, and exhibitions.  
This project simulates a real-world gallery or museum management system focused on structured data organization and efficient record handling.

---

## 🚀 Features

- Artwork, artist, and exhibition record management  
- Add, update, delete, and search database records (CRUD operations)  
- SQL-based data retrieval and filtering  
- Structured relational database design  
- Command-line interface for administrative operations  

---

## 🧠 System Design

The system is built around a **relational database schema** designed to maintain:

- Artwork details (title, year, category, etc.)  
- Artist information  
- Exhibition records  
- Relationships between entities (foreign keys)

A **Python CLI application** interacts with the database, executing SQL queries for record management and retrieval.

---

## 🛠 Technologies Used

- **Python**
- **MySQL**
- SQL queries for data management
- Command-line interface (CLI)
